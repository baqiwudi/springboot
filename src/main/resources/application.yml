spring:
  profiles:
    active: uat
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/boy
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true